# axuanWEB
```bash

conda create -n webserver python=3.9.12
conda activate webserver
pip install -r requirement.txt

which python3
# /home/qqbot/.conda/envs/webserver/bin/python3
vi webstart   # change the first column to the output.

mkdir logs
# unzip static.zip   # yourself's data
# make sure that you have sudo permission.
./webstart
```
